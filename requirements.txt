fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlmodel==0.0.22
python-dotenv==1.0.1
garminconnect==0.2.25
# Pinned explicitly (kept visible); must match what garminconnect requires (0.5.2).
garth==0.5.2
# .fit parsing for per-activity time-series (HR, later elevation/track).
fitdecode==0.11.0
